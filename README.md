# Future Smith

### How to install
```sh
$ git clone https://github.com/starforce86/futures.git
$ composer update
$ composer dumpauto
$ git submodule init
$ git submodule update --remote
$ php artisan migrate
$ php artisan db:seed
```
